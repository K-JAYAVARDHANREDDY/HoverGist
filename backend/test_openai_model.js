const { AzureOpenAI } = require('openai'); const client = new AzureOpenAI({ apiKey: 'test', endpoint: 'https://test.openai.azure.com/', deployment: 'test-dep', apiVersion: '2024-02-15-preview' }); async function run() { try { await client.chat.completions.create({ messages: [{role: 'user', content: 'hi'}], max_tokens: 10 }); console.log('success'); } catch (e) { console.error('ERROR NAME:', e.name); console.error('ERROR MESSAGE:', e.message); } } run();
